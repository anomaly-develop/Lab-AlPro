#include <iostream>
#include <vector>
using namespace std;

int main() {
//   //vector<string> mahasiswa; //vektor kosong
//    vector<string> mahasiswa2 = {"Alice", "Bob", "Charlie", "David", "Eve"}; //inisialisasi vektor dengan nilai awal
//   //vector<string> mahasiswa3(5); //tentukan ukuran vektor di awal "" (kosong)
   vector <int> nilai = {90, 85, 78, 92, 88}; //vektor dengan tipe data int dan inisialisasi nilai awal

//     cout << "jumlah nilai :" <<mahasiswa2.size() << endl; //menampilkan jumlah elemen dalam vektor
//     cout << "nilai ke-2 :" << nilai[1] << endl; //akses elemen ke-2 (indeks 1)
//     cout << "nilai ke-3 :" << mahasiswa2.at(2) << endl; //akses elemen ke-3 (indeks 2) dengan metode at()
//     cout << "cek nilai pertama dengan front() :" << nilai.front() << endl; //akses elemen pertama dengan metode front()
//     cout << "cek nilai terakhir dengan back() :" << nilai.back() << endl; //akses elemen terakhir dengan metode back()

cout << "sebelum diubah :" << endl;
cout << "Nilai ke-1 :" << nilai[0] << endl;
cout << "nilai ke-2 :" << nilai[1] << endl; 
nilai[1] = 95; //mengubah nilai pake []
nilai.at(2) = 80; //mengubah nilai pake at()
nilai.front() = 92; //mengubah nilai pake front()
nilai.back() = 90; //mengubah nilai pake back()
cout << "setelah diubah :" << endl;
cout << "Nilai ke-1 :" << nilai[0] << endl;
cout << "nilai ke-2 :" << nilai[1] << endl;

}